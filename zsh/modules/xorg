#!/bin/zsh
startx() command startx "$XINITRC" -- "$XSERVERRC" &>"$XDG_RUNTIME_DIR/Xorg/:$((XDG_VTNR-1))"
alias \
	lp='spawn leafpad' \
	gp='spawn gpicview' \
	gn='spawn geany' \
	fr='spawn file-roller' \
	kr='spawn krita' \
	za='spawn zathura' \
