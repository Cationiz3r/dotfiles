[module/temperature]
inherit = theme/common_even
type = internal/temperature
interval = 1
thermal-zone = 1
warn-temperature = 70
units = true

format-suffix = "  "
format-warn-suffix = ${module/temperature.format-suffix}

label = %temperature-c%
label-warn = ${module/temperature.label}
